- if session_rabbit.session.language == "rbt"
    #larry{data:{controller: "rabbit", session_uuid: session_rabbit.session.uuid,rabbit_uuid: session_rabbit.uuid}}
        = image_tag "rabbits/larry.png"
        = render partial: 'elements/rabbit_transcriptor', locals: {session_rabbit: session_rabbit}
        = turbo_frame_tag "#{session_rabbit.uuid}-#{session_rabbit.session.uuid}-speech", class: "rabbit-speech" do
            = render partial: "elements/speech_status", locals: {session_rabbit: session_rabbit}