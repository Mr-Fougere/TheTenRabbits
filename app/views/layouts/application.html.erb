<!DOCTYPE html>
<html>
  <head>
    <title>TheTenRabbits</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
  </head>

  <body data-controller="responsive">
    <div data-controller="scream" data-key="<%= @steevie_key%>" data-uuid="<%= @steevie_uuid%>"></div>
    <div  data-controller="seek" data-key="<%= @debbie_key%>" data-uuid="<%= @debbie_uuid%>"> </div>
    <%= yield %>
  </body>
</html>
