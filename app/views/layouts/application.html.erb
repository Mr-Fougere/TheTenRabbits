<!DOCTYPE html>
<html>
  <head>
    <title>TheTenRabbits</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
  </head>

  <body data-controller="responsive">
    <div data-controller="scream" data-key="<%= @credentials[:steevie][:key]%>" data-uuid="<%= @credentials[:steevie][:uuid]%>"></div>
    <div  data-controller="seek" data-key="<%= @credentials[:debbie][:key]%>" data-uuid="<%= @credentials[:debbie][:uuid]%>"> </div>
    <%= yield %>
  </body>
</html>
